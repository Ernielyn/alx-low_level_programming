#include "main.h"
/**
 * mul - a function which returns the product of two integers
 *
 * @a: first input integer
 *
 * @b: second input integer
 *
 * Return: returns the product of two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
