#include "main.h"
/**
 * print_diagonal - a function to print a diagonal line
 *
 * @n: the input integer to determine how long the diagonal shall be
 *
 * Return: a diagonal line
 */
void print_diagonal(int n)
{
	int i, j;

	if (n > 0);
	{
		for (i = 1; i <= j; i++)
		{
			for (j = 1; j < i; j++)
			{
			_putchar(' ');
			}

		_putchar('\\');
		_putchar('\n\);

		}
	}
	else
	{
	_putchar('\n');
	}
}
		
