#include "main.h"
#include <string.h>

/**
 * _strlen - a function toreturn the length of a string
 *
 * @s: and input variable of character type
 *
 * Return: length of the string
 */

int _strlen(char *s)
{
	int n;

	n = strlen(s);

	return (n);
}
